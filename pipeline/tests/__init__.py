"""Test suite. Fixtures only - no test makes a live network call. Contract
tests assert the REAL committed data/*.json files carry every field the
site's JS reads (see test_site_contract.py and DECISIONS.md, 2026-07-11)."""
